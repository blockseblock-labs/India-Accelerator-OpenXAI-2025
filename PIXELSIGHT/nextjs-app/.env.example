# Ollama connection for PixelSight
# Set to your local or remote Ollama base URL
# OLLAMA_HOST=http://127.0.0.1:11434

# Vision model to use (must support images)
# Examples: llava:latest, qwen2.5-vl:latest
# OLLAMA_VISION_MODEL=llava:latest

# Optional: a smaller fallback model if the primary exceeds memory
# Examples: moondream:latest
# OLLAMA_VISION_FALLBACK_MODEL=moondream:latest

# Optional: Increase timeout (ms) if first run is slow due to model warm-up
# OLLAMA_TIMEOUT_MS=120000
